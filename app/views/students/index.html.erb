<h1>All Students</h1>

<ul>
  <% @students.sort_by(&:name).each do |student| %>
    <li><%= link_to student.name, student_path(student) %><%= link_to "Delete", student_path(student), method: :delete %></li>
  <% end %>
</ul>
